import 'package:flutter/material.dart';

class Contactfragment extends StatelessWidget{

  @override
  Widget build(BuildContext context){
    return Container(
      child: Container(
        child: Text("contact fregment"),
      ),
    );
  }
  
}